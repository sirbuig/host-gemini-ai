swagger: "2.0"
info:
  title: PDF Quiz Generator API
  description: API for generating multiple-choice questions from PDFs.
  version: "1.0.0"
host: "34.65.214.77"
schemes:
  - http
consumes:
  - application/json
produces:
  - application/json
